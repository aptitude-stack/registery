name: Dev
color: "#346AB2"
variables:
  - name: baseUrl
    value: http://localhost:8000
  - name: readToken
    value: reader-token.dev-reader-secret
  - name: publishToken
    value: publisher-token.dev-publisher-secret
  - name: reviewToken
    value: admin-token.dev-admin-secret
  - name: adminToken
    value: admin-token.dev-admin-secret
  # Supply a configured telemetry-scoped service token for the star requests.
  - name: telemetryToken
    value: ""
